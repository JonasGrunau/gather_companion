name: gather_events
description: Shared event and presence models for the Gather V2 bridge and companion app.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
